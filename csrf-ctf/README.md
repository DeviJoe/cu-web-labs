# CSRF CTF Challenge - Student Web Application

Веб-приложение для изучения уязвимости Cross-Site Request Forgery (CSRF) в рамках студенческого CTF.

## 🎯 Описание

Это учебное веб-приложение для управления профилями пользователей, содержащее намеренно внедренную уязвимость CSRF. Студенты должны найти способ получить флаг, который хранится в профиле администратора.

**Формат флага:** `centralctf{...}`

## 🚀 Быстрый старт

### Запуск сервиса

```bash
cd examples/csrf-ctf
docker-compose -f docker-compose.simple.yml up --build -d
```

### Доступ к приложению

Откройте в браузере: **http://localhost:5000**

### Проверка работоспособности

```bash
./test_service.sh
```

### Остановка сервиса

```bash
docker-compose -f docker-compose.simple.yml down
```

## 📋 Функционал приложения

- ✅ **Регистрация и вход** - создание учетных записей пользователей
- ✅ **Управление профилем** - обновление био и веб-сайта
- ✅ **Просмотр пользователей** - список зарегистрированных пользователей
- ✅ **Отправка жалоб** - возможность отправить ссылку администратору на проверку

## 🎓 Цель задания

Найти способ получить доступ к **флагу**, который находится в профиле администратора.

### Подсказки для начала

1. Зарегистрируйтесь и изучите все функции приложения
2. Обратите внимание на форму обновления профиля
3. Подумайте, что происходит, когда администратор проверяет отправленные ссылки
4. Вспомните о классических веб-уязвимостях

## 🐳 Варианты развертывания

### Вариант 1: Simple Version (Рекомендуется)

Облегченная версия с минимальными зависимостями. Работает на всех платформах, включая ARM (Mac M1/M2).

```bash
docker-compose -f docker-compose.simple.yml up --build -d
```

**Преимущества:**
- ✅ Быстрая сборка (~10 секунд)
- ✅ Работает на всех архитектурах
- ✅ Минимальное потребление ресурсов (~50MB RAM)

### Вариант 2: Playwright Version

Версия с полноценной браузерной автоматизацией (только x86_64).

```bash
docker-compose -f docker-compose.playwright.yml up --build -d
```

**Требования:**
- ⚠️ Только x86_64 архитектура
- ⚠️ Более длительная сборка (~2 минуты)
- ⚠️ Больше ресурсов (~500MB RAM)

### Вариант 3: Selenium Version

Альтернативная версия с Selenium.

```bash
docker-compose up --build -d
```

### Ручная установка

```bash
# Установка зависимостей
pip install -r requirements.simple.txt

# Запуск приложения
python app.py
```

## 🔧 Конфигурация

### Изменение флага

Отредактируйте `docker-compose.simple.yml`:

```yaml
environment:
  - FLAG=centralctf{your_custom_flag_here}
```

Перезапустите сервис:

```bash
docker-compose -f docker-compose.simple.yml up --build -d
```

### Изменение порта

Если порт 5000 занят, измените в `docker-compose.simple.yml`:

```yaml
ports:
  - "8080:5000"  # Используйте порт 8080
```

## 📊 Требования

- **Docker** и **Docker Compose**
- Свободный порт 5000 (или другой указанный)
- Минимум 100MB свободного места на диске
- Работает на: Linux, macOS (включая ARM), Windows

## 🛠️ Устранение неполадок

### Проблемы со сборкой на ARM (Mac M1/M2)

Используйте упрощенную версию:

```bash
docker-compose -f docker-compose.simple.yml up --build -d
```

### Порт уже используется

```bash
# Найти процесс на порту 5000
lsof -i :5000

# Или измените порт в docker-compose файле
```

### База данных не создается

Очистите и пересоздайте:

```bash
docker-compose -f docker-compose.simple.yml down -v
docker-compose -f docker-compose.simple.yml up --build -d
```

### Просмотр логов

```bash
docker-compose -f docker-compose.simple.yml logs -f
```

## 📚 Документация

- **[QUICKSTART.md](QUICKSTART.md)** - Быстрое руководство для студентов
- **[SOLUTION.md](SOLUTION.md)** - Решение задания (только для преподавателей!)
- **[PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - Подробная структура проекта
- **[exploit_example.html](exploit_example.html)** - Примеры эксплойтов

## 🔐 Информация для преподавателей

### Учетные данные администратора

- **Username:** `admin`
- **Password:** `admin_secret_password_123`
- **Флаг:** Хранится в поле `bio` администратора

**⚠️ Не раскрывайте пароль администратора студентам!**

### Флаг по умолчанию

```
centralctf{csrf_1s_n0t_d3ad_yet}
```

### Уязвимость

Endpoint `/update_profile` не имеет защиты от CSRF атак:
- Отсутствуют CSRF токены
- Нет проверки заголовков Origin/Referer
- Автоматическая отправка cookies браузером

### Механизм эксплуатации

1. Студент создает вредоносную HTML страницу
2. Страница содержит форму, которая автоматически отправляет POST запрос
3. Студент отправляет URL через функцию "Report"
4. Админ-бот посещает страницу, находясь в авторизованной сессии
5. Форма автоматически отправляется, изменяя профиль администратора
6. Студент может увидеть измененный профиль в списке пользователей

### Решение

Полное решение с примерами эксплойтов находится в файле **[SOLUTION.md](SOLUTION.md)**.

## 📖 Учебные материалы

### Рекомендуемые ресурсы

- [OWASP - Cross-Site Request Forgery (CSRF)](https://owasp.org/www-community/attacks/csrf)
- [PortSwigger - CSRF Tutorial](https://portswigger.net/web-security/csrf)
- [MDN - HTTP Cookies](https://developer.mozilla.org/en-US/docs/Web/HTTP/Cookies)
- [Flask Security](https://flask.palletsprojects.com/en/latest/security/)

### Темы для изучения

- Cross-Site Request Forgery (CSRF)
- Session management
- Cookie security (SameSite attribute)
- CSRF tokens
- Same-Origin Policy
- HTTP methods (GET vs POST)

## 🎯 Критерии оценки

Студент успешно выполняет задание, если:

- ✅ Идентифицирует уязвимость CSRF
- ✅ Понимает работу админ-бота
- ✅ Создает работающий эксплойт
- ✅ Получает флаг
- ✅ Может объяснить механизм атаки и защиты

## 🔒 Защита от CSRF

Примеры правильной защиты (НЕ реализовано в этом задании):

### 1. CSRF токены (Flask-WTF)

```python
from flask_wtf import CSRFProtect

csrf = CSRFProtect(app)
```

### 2. SameSite cookies

```python
app.config['SESSION_COOKIE_SAMESITE'] = 'Lax'
app.config['SESSION_COOKIE_SECURE'] = True
```

### 3. Проверка Origin/Referer

```python
@app.before_request
def check_referer():
    if request.method == 'POST':
        referer = request.headers.get('Referer')
        if not referer or 'yourdomain.com' not in referer:
            abort(403)
```

### 4. Custom headers для AJAX

```javascript
fetch('/api/endpoint', {
    method: 'POST',
    headers: {
        'X-Custom-Header': 'value',
        'Content-Type': 'application/json'
    }
})
```

## ⚠️ Важные замечания

1. **Это учебное приложение** с намеренными уязвимостями
2. **НЕ используйте в production** окружении
3. **НЕ разворачивайте** в публичных сетях
4. Предназначено только для **образовательных целей**
5. Используйте в **изолированных сетях** или локально

## 🏗️ Технический стек

- **Backend:** Flask (Python 3.11)
- **Database:** SQLite3
- **Frontend:** HTML5 + CSS3 (Vanilla)
- **Admin Bot:** Python requests / Selenium / Playwright
- **Deployment:** Docker + Docker Compose

## 📊 Статистика проекта

```
Файлов: 24
Строк кода: ~1500
Endpoints: 8
Templates: 7
Docker configs: 3
```

## 🤝 Вклад в проект

Это учебный проект для CTF соревнований. Если вы нашли баги или хотите улучшить материалы:

1. Опишите проблему или улучшение
2. Убедитесь, что изменения не упрощают задание
3. Протестируйте на всех платформах

## 📝 Лицензия

Этот проект предназначен для образовательных целей в рамках CTF соревнований.

## 👥 Поддержка

### Для студентов

- Изучите документацию
- Используйте подсказки в `QUICKSTART.md`
- Экспериментируйте с функционалом
- НЕ смотрите `SOLUTION.md` до завершения задания!

### Для преподавателей

- Смотрите `SOLUTION.md` для полного решения
- Используйте `test_service.sh` для проверки работоспособности
- Изменяйте флаг для разных групп студентов
- Мониторьте логи при необходимости

## 🎉 Начало работы

```bash
# 1. Клонируйте или скопируйте проект
cd examples/csrf-ctf

# 2. Запустите сервис
docker-compose -f docker-compose.simple.yml up --build -d

# 3. Проверьте работоспособность
./test_service.sh

# 4. Откройте в браузере
open http://localhost:5000

# 5. Начните исследование!
```

---

**Удачи в прохождении CTF! 🚀**

**Помните:** Цель - не только найти флаг, но и понять, как работает уязвимость и как от неё защититься!