# CSRF CTF Challenge - Quick Start Guide

## Быстрый запуск (5 минут)

### 1. Запустите сервис

```bash
cd examples/csrf-ctf
docker-compose -f docker-compose.simple.yml up --build -d
```

### 2. Откройте приложение

Перейдите по адресу: http://localhost:5000

### 3. Зарегистрируйтесь

- Создайте аккаунт
- Войдите в систему
- Изучите функционал

### 4. Цель задания

Найти способ получить доступ к **флагу**, который хранится в профиле администратора.

**Формат флага:** `centralctf{...}`

---

## Что доступно в приложении?

- ✅ Регистрация и вход
- ✅ Обновление профиля (bio, website)
- ✅ Просмотр списка пользователей
- ✅ Отправка ссылок администратору на проверку

---

## Подсказки для начала

1. Исследуйте все функции приложения
2. Обратите внимание на форму обновления профиля
3. Подумайте, что делает администратор, когда вы отправляете ему ссылку
4. Вспомните о классических веб-уязвимостях

---

## Остановка сервиса

```bash
docker-compose -f docker-compose.simple.yml down
```

---

## Просмотр логов

```bash
docker-compose -f docker-compose.simple.yml logs -f
```

---

## Смена флага

Отредактируйте `docker-compose.simple.yml`:

```yaml
environment:
  - FLAG=centralctf{your_custom_flag_here}
```

Затем перезапустите:

```bash
docker-compose -f docker-compose.simple.yml up --build -d
```

---

## Требования

- Docker и Docker Compose
- Порт 5000 должен быть свободен
- Работает на всех платформах (Linux, macOS, Windows)

---

## Устранение неполадок

### Порт 5000 занят

Измените порт в `docker-compose.simple.yml`:

```yaml
ports:
  - "8080:5000"  # Используйте 8080 вместо 5000
```

### Контейнер не запускается

Проверьте логи:

```bash
docker-compose -f docker-compose.simple.yml logs
```

Пересоберите:

```bash
docker-compose -f docker-compose.simple.yml down
docker-compose -f docker-compose.simple.yml up --build
```

### База данных не создается

Очистите все и пересоздайте:

```bash
docker-compose -f docker-compose.simple.yml down -v
docker-compose -f docker-compose.simple.yml up --build -d
```

---

## Для преподавателей

- **Решение**: см. файл `SOLUTION.md`
- **Флаг по умолчанию**: `centralctf{csrf_1s_n0t_d3ad_yet}`
- **Пароль админа**: `admin_secret_password_123` (не раскрывать студентам!)

---

## Технические детали

- **Backend**: Flask (Python)
- **Database**: SQLite
- **Admin Bot**: Автоматизированный с помощью requests library
- **Уязвимость**: CSRF (Cross-Site Request Forgery)

---

## Дополнительные материалы

- [OWASP CSRF](https://owasp.org/www-community/attacks/csrf)
- [PortSwigger CSRF Tutorial](https://portswigger.net/web-security/csrf)
- [Flask Security Best Practices](https://flask.palletsprojects.com/en/latest/security/)

---

## Поддержка

Если возникли вопросы или проблемы:

1. Проверьте логи контейнера
2. Убедитесь, что порт 5000 свободен
3. Попробуйте пересобрать контейнер
4. Проверьте, что Docker daemon запущен

---

**Удачи в прохождении CTF!** 🚀