# 🎯 CTF Задание: XML Parser Service

## Описание задания

Вам предоставлен доступ к веб-сервису, который парсит XML документы и возвращает обработанные данные.

**Ваша цель:** Найти флаг формата `centralctf{...}`, который хранится где-то на сервере.

## Доступ к сервису

После запуска сервис будет доступен по адресу:
```
http://localhost:5000
```

## Что известно

1. Сервис принимает XML документы через POST запрос на `/api/parse`
2. Сервис обрабатывает XML и возвращает результат в формате JSON
3. Флаг хранится на файловой системе сервера
4. Формат флага: `centralctf{...}`

## Доступные endpoints

- `GET /` - Главная страница с веб-интерфейсом
- `POST /api/parse` - API для парсинга XML (основной endpoint)
- `GET /api/example` - Пример валидного XML запроса

## Пример использования

### Через веб-интерфейс
1. Откройте http://localhost:5000
2. Введите XML в текстовое поле
3. Нажмите "Отправить XML"
4. Изучите ответ сервера

### Через curl
```bash
curl -X POST http://localhost:5000/api/parse \
  -H "Content-Type: application/xml" \
  -d '<?xml version="1.0" encoding="UTF-8"?>
<user>
    <name>Test</name>
    <email>test@example.com</email>
</user>'
```

## Что нужно сделать

1. **Исследуйте сервис** - Попробуйте отправить различные XML документы
2. **Изучите ответы** - Обратите внимание, как сервер обрабатывает XML
3. **Найдите уязвимость** - Подумайте, как можно использовать функциональность XML для доступа к файлам на сервере
4. **Получите флаг** - Используйте найденную уязвимость для чтения файла с флагом

## Подсказки

- XML - это не просто формат данных, он имеет мощные возможности
- Изучите спецификацию XML, особенно возможности Document Type Definition (DTD)
- Подумайте, как можно взаимодействовать с файловой системой через XML
- Поиск в интернете разрешен! Ищите информации об уязвимостях XML парсеров

## Оценка

- **Сложность:** Легкая / Средняя
- **Категория:** Web Security
- **Очки:** 150-200 (зависит от вашего CTF)
- **Ожидаемое время:** 15-45 минут

## Правила

✅ Разрешено:
- Использовать любые инструменты (curl, Burp Suite, скрипты)
- Искать информацию в интернете
- Отправлять любые XML документы на сервер
- Экспериментировать с различными подходами

❌ Запрещено:
- Атаковать инфраструктуру хостинга
- Проводить DoS атаки
- Пытаться получить shell на сервере
- Заглядывать в папку `organizers-only/` (это читерство!)

## Формат ответа

После нахождения флага, отправьте его в систему проверки в точном формате:
```
centralctf{...}
```

## Нужна помощь?

Если вы застряли:
1. Перечитайте задание внимательно
2. Попробуйте поискать в интернете информацию про "XML security vulnerabilities"
3. Обратите внимание на то, как сервер обрабатывает различные XML конструкции
4. Подумайте: какие дополнительные возможности есть у XML, кроме простого хранения данных?

## Дополнительная информация

- Документация проекта: `README.md`
- Быстрый старт: `QUICKSTART.md`
- Краткая справка: `INFO.txt`

---

**Удачи! 🚀**

*P.S. После решения задания обязательно изучите правильное решение и методы защиты от найденной уязвимости!*