# Руководство для организаторов CTF

## 🎯 О челлендже

**Название**: SQL Injection Challenge with WAF Bypass  
**Категория**: Web Security  
**Сложность**: Medium/Hard  
**Тип уязвимости**: Blind SQL Injection + WAF Bypass  
**Порт**: 5050  
**Флаг**: `centralctf{bl1nd_5ql_w1th_w4f_byp4ss_m4st3r}`

## 🚀 Быстрый запуск

```bash
# Запуск сервиса
docker-compose up -d

# Проверка работы
curl http://localhost:5050/health

# Запуск эксплойта (для проверки)
pip3 install requests
python3 exploit.py
```

## 📁 Файлы для участников

### ✅ МОЖНО раздавать участникам:
- URL сервиса: `http://your-server:5050`
- `README.md` (опционально, для описания сервиса)
- `docker-compose.yml` и `Dockerfile` (если хотите дать исходники)

### ❌ НЕ РАЗДАВАТЬ участникам:
- `SOLUTION.md` - полное решение с флагом
- `exploit.py` - автоматический эксплойт
- `MANUAL_TESTING.md` - примеры ручной эксплуатации
- `ORGANIZERS_GUIDE.md` - это руководство
- `QUICKSTART.md` - содержит флаг

## 🔐 Информация о челлендже

### Описание для участников

```
Название: User Directory Search

Описание:
Перед вами простой веб-сервис для поиска пользователей по ID.
Где-то в системе спрятан флаг формата centralctf{...}
Ваша задача - найти способ его извлечь.

URL: http://your-server:5050
Формат флага: centralctf{...}

Подсказка: Обратите внимание на параметр поиска.
```

### Техническая информация

**Уязвимость:**
- Blind SQL Injection в параметре `id` эндпоинта `/search`
- Отсутствие параметризованных запросов
- Прямая конкатенация пользовательского ввода в SQL

**Защита:**
- Простой WAF блокирует SQL ключевые слова:
  - SELECT, UNION, OR, AND, WHERE, FROM, INSERT, DELETE, DROP, UPDATE
  - Комментарии: --, /*, */
  - Другие опасные паттерны

**Обход WAF:**
- URL encoding символов (WAF не декодирует перед проверкой)
- Например: `AND` → `%41%4e%44`

**База данных:**
- SQLite
- Таблица `users`: обычные пользователи (id 1-4)
- Таблица `secrets`: содержит флаг (secret_key='flag')

## 🎓 Обучающие цели

Участники научатся:
1. Обнаруживать Blind SQL Injection
2. Обходить WAF через URL encoding
3. Извлекать данные побайтово через Boolean-based SQLi
4. Использовать функции SQLite (substr, unicode, length)
5. Оптимизировать атаку через binary search

## 📊 Оценка сложности

- **Обнаружение уязвимости**: Easy
- **Обнаружение WAF**: Easy  
- **Обход WAF**: Medium
- **Извлечение флага**: Medium/Hard
- **Оптимизация**: Hard

**Ожидаемое время решения:**
- Начинающие: 2-4 часа
- Средний уровень: 1-2 часа
- Опытные: 30-60 минут

## 💡 Система подсказок

### Подсказка 1 (через 30 минут)
```
Попробуйте разные значения параметра 'id' в URL.
Что происходит с нестандартными значениями?
```

### Подсказка 2 (через 1 час)
```
Приложение защищено WAF (Web Application Firewall).
Как можно обойти фильтры ключевых слов?
Подумайте о различных способах кодирования данных в URL.
```

### Подсказка 3 (через 1.5 часа)
```
URL encoding может помочь обойти WAF.
Попробуйте закодировать SQL ключевые слова:
- Пробел = %20
- A = %41
- N = %4e
- D = %44
```

### Подсказка 4 (через 2 часа)
```
Это Blind SQL Injection - вы не видите данные напрямую,
только True/False ответы (User Found / User Not Found).

Используйте функции SQLite для извлечения данных побайтово:
- substr(text, position, length) - извлечь подстроку
- length(text) - длина строки
- unicode(char) - ASCII код символа

Данные находятся в таблице secrets, колонка secret_value.
```

### Подсказка 5 (через 2.5 часа) - финальная
```
Пример payload для проверки первого символа 'c':
1 AND (select 1 from secrets where substr(secret_value,1,1)='c')=1

Закодируйте каждый символ через URL encoding!
```

## 🔧 Настройка и управление

### Смена флага

Отредактируйте `app.py`, строка ~60:
```python
cursor.execute("""
    INSERT INTO secrets (id, secret_key, secret_value)
    VALUES (1, 'flag', 'centralctf{ваш_новый_флаг_здесь}')
""")
```

Затем пересоберите:
```bash
docker-compose down
docker-compose up -d --build
```

### Смена порта

В `docker-compose.yml`:
```yaml
ports:
  - "ваш_порт:5050"
```

В `app.py` менять не нужно (внутренний порт остается 5050).

### Мониторинг

```bash
# Логи в реальном времени
docker-compose logs -f

# Статус контейнера
docker-compose ps

# Использование ресурсов
docker stats ctf-sqli-service
```

### Защита от DoS

Добавьте rate limiting через nginx:

```nginx
limit_req_zone $binary_remote_addr zone=sqli:10m rate=30r/m;

server {
    location / {
        limit_req zone=sqli burst=10 nodelay;
        proxy_pass http://localhost:5050;
    }
}
```

### Изоляция команд (multiple instances)

```bash
# Для каждой команды свой порт и контейнер
for team_id in {1..10}; do
    port=$((5050 + team_id))
    docker run -d \
        --name ctf-sqli-team-${team_id} \
        -p ${port}:5050 \
        ctf-sqli-service
done
```

## 📈 Метрики успеха

### Статистика для отслеживания:
- Количество решивших команд
- Среднее время решения
- Использование подсказок
- Популярные методы решения

### Логирование попыток (опционально)

Добавьте в `app.py` логирование подозрительных запросов:
```python
import logging

# Настройка логгера
logging.basicConfig(
    filename='attempts.log',
    level=logging.INFO,
    format='%(asctime)s - %(message)s'
)

# В функции search():
if waf_check(user_id):
    logging.info(f"WAF blocked: {user_id} from {request.remote_addr}")
```

## 🐛 Troubleshooting

### Проблема: Контейнер не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose logs

# Проверьте порт
netstat -an | grep 5050

# Пересоберите
docker-compose down
docker-compose up -d --build
```

### Проблема: Участники жалуются на "слишком медленно"

**Причина:** Используют линейный поиск вместо binary search

**Решение:** Дайте подсказку про функцию `unicode()` и бинарный поиск

### Проблема: База данных повреждена

**Решение:**
```bash
docker-compose down -v
rm -rf data/
docker-compose up -d
```

### Проблема: Слишком много запросов (DoS)

**Решение:**
```bash
# Ограничьте ресурсы контейнера
docker-compose down
```

Добавьте в `docker-compose.yml`:
```yaml
deploy:
  resources:
    limits:
      cpus: '0.5'
      memory: 256M
```

```bash
docker-compose up -d
```

## 🎯 Валидация флага

Проверка формата флага от участников:
```python
import re

def validate_flag(flag):
    pattern = r'^centralctf\{[a-zA-Z0-9_]+\}$'
    return bool(re.match(pattern, flag))

# Правильный флаг
correct_flag = "centralctf{bl1nd_5ql_w1th_w4f_byp4ss_m4st3r}"
```

## 📝 Checklist перед запуском CTF

- [ ] Сервис запущен и доступен
- [ ] Health check работает
- [ ] Флаг извлекается через exploit.py
- [ ] SOLUTION.md и exploit.py НЕ опубликованы
- [ ] Rate limiting настроен (если нужно)
- [ ] Мониторинг логов настроен
- [ ] Подсказки подготовлены
- [ ] Система валидации флагов работает
- [ ] Резервные копии конфигурации созданы

## 🏆 Критерии оценки

### Полное решение (100 баллов):
- Обнаружена уязвимость: 20 баллов
- Обнаружен WAF: 10 баллов
- Найден способ обхода WAF: 30 баллов
- Флаг извлечен: 40 баллов

### Бонусы (опционально):
- Автоматизация с binary search: +10 баллов
- Скрипт эксплуатации: +10 баллов
- Оригинальный метод обхода: +5 баллов

## 📞 Поддержка участников

### FAQ для участников:

**Q: Что делать если "Malicious input detected"?**  
A: Это означает, что WAF заблокировал ваш запрос. Подумайте о способах обхода фильтров.

**Q: Приложение всегда возвращает "User Not Found"**  
A: Проверьте синтаксис вашего SQL запроса. Возможно, есть ошибка.

**Q: Это занимает слишком много времени!**  
A: Попробуйте оптимизировать ваш подход. Binary search быстрее линейного поиска.

**Q: Можно использовать sqlmap?**  
A: Да, но вам придется настроить его для обхода WAF.

## 🔗 Полезные ссылки для участников

- [OWASP SQL Injection](https://owasp.org/www-community/attacks/SQL_Injection)
- [SQLite Core Functions](https://www.sqlite.org/lang_corefunc.html)
- [URL Encoding Reference](https://www.w3schools.com/tags/ref_urlencode.asp)
- [Blind SQL Injection Tutorial](https://portswigger.net/web-security/sql-injection/blind)

## 📄 Пример отчета решения

```markdown
# Solution Report: SQL Injection Challenge

## Vulnerability Discovery
Found SQL injection in /search?id= parameter

## WAF Detection
Application blocks keywords: OR, AND, SELECT, FROM, etc.

## WAF Bypass
Used URL encoding to bypass keyword filters:
- AND → %41%4e%44
- select → %73%65%6c%65%63%74

## Exploitation
Used Blind SQL Injection with boolean-based technique:
- Extracted flag character by character
- Used substr() and unicode() functions
- Implemented binary search for optimization

## Flag
centralctf{bl1nd_5ql_w1th_w4f_byp4ss_m4st3r}

## Tools Used
- Python requests library
- Custom exploit script
- curl for manual testing
```

---

**Удачи на CTF! 🚩**

*При возникновении вопросов обращайтесь к файлу SOLUTION.md для полного технического решения.*