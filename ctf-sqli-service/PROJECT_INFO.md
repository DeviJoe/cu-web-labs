# CTF SQL Injection Service - Project Overview

## 📋 Описание проекта

Это полноценный CTF-сервис для обучения студентов SQL Injection атакам и обходу WAF (Web Application Firewall). Сервис представляет собой веб-приложение на Flask с намеренно внедренной уязвимостью Blind SQL Injection.

## 🎯 Основные характеристики

- **Тип уязвимости**: Blind SQL Injection
- **Защита**: WAF блокирующий SQL ключевые слова
- **Техника обхода**: URL Encoding
- **База данных**: SQLite
- **Порт**: 5050
- **Флаг**: `centralctf{bl1nd_5ql_w1th_w4f_byp4ss_m4st3r}`

## 📂 Структура проекта

```
ctf-sqli-service/
├── app.py                      # Flask приложение с уязвимостью
├── requirements.txt            # Python зависимости
├── Dockerfile                  # Docker образ
├── docker-compose.yml          # Docker Compose конфигурация
├── deploy.sh                   # Скрипт автоматического развертывания
├── test_service.sh             # Скрипт тестирования сервиса
├── templates/
│   ├── index.html             # Главная страница
│   └── search.html            # Страница результатов
│
├── README.md                   # Основная документация
├── QUICKSTART.md              # Быстрый старт
├── ORGANIZERS_GUIDE.md        # Руководство для организаторов
├── PROJECT_INFO.md            # Этот файл
│
└── ⚠️ ПРИВАТНЫЕ ФАЙЛЫ (не для участников):
    ├── SOLUTION.md            # Полное решение с флагом
    ├── exploit.py             # Автоматический эксплойт
    └── MANUAL_TESTING.md      # Примеры ручной эксплуатации
```

## 🚀 Быстрый старт

### Для организаторов:

```bash
# 1. Запуск сервиса
docker-compose up -d

# 2. Проверка работоспособности
./test_service.sh

# 3. Тестирование эксплойта
pip3 install requests
python3 exploit.py
```

### Для участников:

Предоставьте только:
- URL: `http://your-server:5050`
- Формат флага: `centralctf{...}`
- Описание: "Веб-сервис для поиска пользователей. Найдите флаг."

## 🔒 Безопасность

### ✅ Можно публиковать:
- `README.md`
- `app.py` (если даете исходники)
- `Dockerfile` и `docker-compose.yml`
- URL сервиса

### ❌ НЕ публиковать:
- `SOLUTION.md` - содержит флаг и решение
- `exploit.py` - автоматический эксплойт
- `MANUAL_TESTING.md` - примеры эксплуатации
- `ORGANIZERS_GUIDE.md` - внутренняя информация
- `QUICKSTART.md` - содержит флаг

## 🎓 Обучающие цели

Студенты научатся:
1. ✅ Обнаруживать SQL Injection уязвимости
2. ✅ Распознавать Blind SQL Injection
3. ✅ Обходить Web Application Firewall через URL encoding
4. ✅ Извлекать данные побайтово
5. ✅ Использовать SQLite функции (substr, unicode, length)
6. ✅ Оптимизировать атаки через binary search

## 🛠️ Технический стек

- **Backend**: Python 3.11 + Flask 2.3.3
- **Database**: SQLite
- **Containerization**: Docker + Docker Compose
- **WAF**: Custom regex-based filter

## 🔍 Уязвимость в деталях

### Уязвимый код (app.py):
```python
query = f"SELECT username, email FROM users WHERE id = {user_id}"
cursor.execute(query)
```

### WAF блокирует:
- SQL keywords: SELECT, UNION, OR, AND, WHERE, FROM
- Comments: --, /*, */
- Hex encoding: 0x...
- Functions: char(), concat(), group_concat()

### Обход WAF:
```python
# Вместо: 1 AND (select 1 from secrets)=1
# Используем: URL encoding каждого символа
payload = "1%20%41%4e%44%20%28%73%65%6c%65%63%74%20%31%20%66%72%6f%6d%20%73%65%63%72%65%74%73%29%3d%31"
```

## 📊 Уровень сложности

- **Категория**: Web Security
- **Сложность**: Medium/Hard
- **Рекомендуемые навыки**:
  - Базовое понимание SQL
  - Знание HTTP протокола
  - Опыт работы с Burp Suite или curl
  - Python (для автоматизации)

**Время решения:**
- Начинающие: 2-4 часа
- Средний уровень: 1-2 часа
- Эксперты: 30-60 минут

## 🧪 Тестирование

### Автоматическое тестирование:
```bash
./test_service.sh
```

### Ручное тестирование:
```bash
# Health check
curl http://localhost:5050/health

# Нормальный запрос
curl "http://localhost:5050/search?id=1"

# WAF блокирует
curl "http://localhost:5050/search?id=1%20OR%201=1"

# Обход WAF (URL encoding)
curl "http://localhost:5050/search?id=1%20%41%4e%44%20%31%3d%31"
```

## 📈 Метрики успеха

Отслеживайте:
- Количество решивших команд
- Среднее время решения
- Количество использованных подсказок
- Популярные методы решения (ручной vs автоматизированный)

## 🔧 Конфигурация

### Изменить флаг:
Отредактируйте `app.py` (строка ~60):
```python
VALUES (1, 'flag', 'ваш_новый_флаг')
```

### Изменить порт:
В `docker-compose.yml`:
```yaml
ports:
  - "новый_порт:5050"
```

### Rate limiting:
Рекомендуется настроить через nginx или traefik:
```
limit_req_zone: 30 запросов в минуту
```

## 📚 Документация

- **README.md** - Основная инструкция по развертыванию
- **QUICKSTART.md** - Самые важные команды
- **ORGANIZERS_GUIDE.md** - Полное руководство для организаторов
- **SOLUTION.md** - Техническое решение (приватно)
- **MANUAL_TESTING.md** - Примеры curl команд (приватно)

## 🐛 Известные проблемы

1. **Производительность**: Linear search медленный, рекомендуйте binary search
2. **DoS атаки**: Установите rate limiting
3. **WAF**: Декодирует только при проверке, можно обойти через encoding

## 🔄 Версионирование

**Текущая версия**: 1.0.0

**История изменений**:
- v1.0.0 - Первый релиз с Blind SQLi + WAF bypass

## 🤝 Для разработчиков

### Запуск локально без Docker:
```bash
pip install -r requirements.txt
python app.py
```

### Тестирование WAF:
```bash
python -c "from app import waf_check; print(waf_check('SELECT'))"
```

### Дебаг режим:
В `app.py` измените:
```python
app.run(host="0.0.0.0", port=5050, debug=True)
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs`
2. Проверьте порт: `netstat -an | grep 5050`
3. Пересоберите: `docker-compose up -d --build`
4. Запустите тесты: `./test_service.sh`

## 📄 Лицензия

Для образовательных целей. Свободное использование на CTF мероприятиях.

⚠️ **ПРЕДУПРЕЖДЕНИЕ**: Этот сервис содержит намеренные уязвимости.
НЕ используйте код из этого проекта в продакшене!

## 🎯 Чек-лист перед CTF

- [ ] Сервис запущен и доступен
- [ ] Тесты пройдены (`./test_service.sh`)
- [ ] Флаг извлекается через `exploit.py`
- [ ] Приватные файлы НЕ опубликованы
- [ ] Rate limiting настроен
- [ ] Мониторинг включен
- [ ] Подсказки подготовлены
- [ ] Backup конфигурации создан

## 🏆 Результаты

После проведения CTF:
- Соберите feedback от участников
- Проанализируйте методы решения
- Обновите подсказки при необходимости
- Поделитесь статистикой успеха

---

**Создано для студенческих CTF соревнований**

**Удачи! 🚩**