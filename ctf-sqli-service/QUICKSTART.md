# Quick Start Guide

## Запуск сервиса (1 команда)

```bash
docker-compose up -d
```

Сервис доступен: **http://localhost:5050**

---

## Для организаторов

### Проверка работы

```bash
# Быстрая проверка (только curl)
chmod +x test_basic.sh
./test_basic.sh

# Или с Python
pip3 install requests
python3 verify.py
```

### Запуск эксплойта

```bash
# Exploit не требует внешних зависимостей (использует только urllib из стандартной библиотеки)
python3 exploit.py
```

### Флаг
```
centralctf{bl1nd_5ql_w1th_w4f_byp4ss_m4st3r}
```

### Важно! 🔒
**НЕ публикуйте файлы:**
- `SOLUTION.md`
- `exploit.py`
- `MANUAL_TESTING.md`

Это решения для организаторов!

---

## Для участников

### Что раздавать участникам:
1. URL сервиса: `http://your-server:5050`
2. Формат флага: `centralctf{...}`
3. Подсказка (опционально): "Ищите уязвимость в функции поиска"

### Минимальная информация:
- Веб-сервис для поиска пользователей
- Где-то спрятан флаг
- Найдите способ его извлечь

---

## Управление

### Остановить
```bash
docker-compose down
```

### Перезапустить
```bash
docker-compose restart
```

### Логи
```bash
docker-compose logs -f
```

### Полная очистка
```bash
docker-compose down -v
rm -rf data/
```

---

## Проверка работы

```bash
# Быстрая проверка всех функций (только curl)
chmod +x test_basic.sh
./test_basic.sh

# Или вручную:

# Health check
curl http://localhost:5050/health

# Нормальный запрос
curl "http://localhost:5050/search?id=1"

# Проверка WAF
curl "http://localhost:5050/search?id=1%20OR%201=1"
# Должно вернуть: "Malicious input detected"

# Проверка bypass через URL encoding
curl "http://localhost:5050/search?id=1%20%41%4e%44%20%31%3d%31"
# Должно вернуть: "User Found"
```

---

## Структура челленджа

- **Уязвимость**: Blind SQL Injection
- **Защита**: WAF (Web Application Firewall)
- **Обход**: URL Encoding
- **Сложность**: Medium/Hard
- **База данных**: SQLite
- **Таблица с флагом**: `secrets`

---

## Подсказки по уровням (если участники застряли)

**Уровень 1**: Попробуйте разные значения параметра `id`

**Уровень 2**: Приложение защищено WAF. Как обойти фильтры?

**Уровень 3**: URL encoding помогает обходить некоторые защиты

**Уровень 4**: Blind SQLi - можно задавать вопросы типа "да/нет"

**Уровень 5**: Используйте функции `substr()` и `unicode()` в SQLite

---

## Смена флага

Отредактируйте в `app.py`:
```python
VALUES (1, 'flag', 'centralctf{ваш_новый_флаг}')
```

Пересоберите:
```bash
docker-compose down
docker-compose up -d --build
```

---

## Системные требования

- Docker + Docker Compose
- 256 MB RAM
- 100 MB диска
- Порт 5050 свободен

---

## Поддержка

Проблемы с запуском? Проверьте:
1. Docker запущен: `docker ps`
2. Порт свободен: `netstat -an | grep 5050`
3. Логи: `docker-compose logs`

---

**Удачи на CTF! 🚩**